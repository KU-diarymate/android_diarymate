<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/hashtag_background"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/background_hashtag"
    android:backgroundTint="@color/green_theme"
    android:paddingTop="2dp"
    android:paddingBottom="2dp"
    android:paddingStart="15dp"
    android:paddingEnd="15dp"
    android:layout_margin="5dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/hashtag_hash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="#"
        android:textColor="@color/white"
        android:textSize="15dp"
        android:fontFamily="@font/pretendard_semi_bold"/>

    <TextView
        android:id="@+id/hashtag_title_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/hashtag_hash"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="피아노"
        android:textColor="@color/white"
        android:textSize="15dp"
        android:fontFamily="@font/pretendard_semi_bold"
        android:layout_marginStart="5dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>